{
  "enable_cmd_vlm_selection": {
    "type": "bool",
    "description": "【指令搜图】(/搜图) 是否启用大模型淘汰比对",
    "default": true
  },
  "enable_nl_search_vlm_selection": {
    "type": "bool",
    "description": "【自然语言搜图】(如“帮我搜张图”) 是否启用大模型淘汰比对",
    "default": true
  },
  "enable_explanation_image": {
    "type": "bool",
    "description": "【解释附图】是否启用功能（解答明确实体时自动配图）",
    "default": true
  },
  "enable_explanation_vlm_selection": {
    "type": "bool",
    "description": "【解释附图】是否启用大模型淘汰比对（默认关闭以提升解答速度）",
    "default": false
  },
  "vlm_provider_id": {
    "type": "string",
    "description": "用于图片比对的 VLM Provider ID。留空则自动使用当前对话的默认模型",
    "default": "",
    "_special": "select_provider"
  },
  "batch_size": {
    "type": "int",
    "description": "单批次抓取及拼图的候选图片数量（建议设置为 4 或 9，最大16）。数量越大图片质量越高，但处理时间会变长。",
    "default": 9
  }
}
